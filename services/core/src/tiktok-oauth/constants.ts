/** Must match TikTok Developer Portal → Login Kit → Redirect URI (Sandbox + Live) exactly. */
export const TIKTOK_OAUTH_REDIRECT_URI_CANONICAL =
  'https://api.kckellie.com/api/analytics/tiktok/oauth/callback';
