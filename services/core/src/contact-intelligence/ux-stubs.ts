/**
 * Deprecated stub surface — real handlers live in ./store.ts.
 * Kept so older imports fail loudly if stub flag is expected.
 */
export {
  CONTACT_INTELLIGENCE_UX_STUB,
  listContactIntelligenceHub,
  getContactBrief,
  getRecommendationBrief,
  recordContactIntelligenceFeedback,
} from './store.js';
