# Benson pre-alpha log retention — 14 days minimum, rotate by size and age.
@BENSON_ROOT@/.logs/pre-alpha/*.log {
  daily
  rotate 14
  maxsize 100M
  compress
  delaycompress
  missingok
  notifempty
  copytruncate
  dateext
  dateformat -%Y%m%d
}
